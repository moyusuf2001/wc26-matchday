# The live data comes from ESPN's free public API — NO API KEY IS REQUIRED.
# You can run the app with no .env.local at all.
#
# Optional: set your live URL in production so the social preview image resolves.
# NEXT_PUBLIC_SITE_URL=https://your-project.vercel.app
